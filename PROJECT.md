# 美团 AI Agent 大赛参赛作品

## 作品名称

**剧本杀 Citywalk × 智能行程 Agent**

## 作品简介

不只是推荐去哪玩，而是把整座城市变成沉浸式游戏场，Agent 做你的 DM（主持人）。

### 核心能力

- **智能路线规划**：贪心 + 2-opt 算法优化多 POI 串联，考虑距离、时间、营业约束
- **LLM 任务生成**：DeepSeek 驱动，自动选择工具、填充参数、声明依赖，规则引擎兜底
- **剧本杀叙事引擎**：4 套故事模板，把普通 Citywalk 变成沉浸式剧情冒险
- **一键执行**：用户确认后自动执行预订、排队、配送等操作
- **三层异常处理**：Retry（指数退避） -> Fallback（备选方案自动执行） -> Replan（环境剧变重规划）

### 技术架构

```
用户输入
  → 路线规划引擎（候选 POI 筛选 → 综合评分 → 贪心构建 → 2-opt 优化）
  → LLM 任务生成（意图 + 路线 + 工具列表 → 任务 DAG）
  → 确认执行（状态机驱动 → 工具调用 → 异常处理 → 结果汇总）
```

- **后端**：Python FastAPI，每次请求创建局部状态机，避免并发串状态
- **前端**：微信小程序，plan → confirm → execute 完整流程
- **数据**：35 个 POI，18 个品类，185 个测试全绿

## 作品链接

- **GitHub 仓库**：https://github.com/wjyxiaojiu-del/meituan-agent
- **快速启动**：双击 `start.bat`（Windows）或运行 `./start.sh`（Mac/Linux）
- **API 文档**：启动后访问 http://localhost:8080/docs

## 演示场景

| 场景 | 输入 | 展示能力 |
|------|------|----------|
| 家庭出游 | 带老婆孩子出去玩，孩子5岁，老婆在减肥，要有惊喜 | 天气+搜索+预订+排队+配送 |
| 剧本杀 Citywalk | 和4个朋友出去玩，想玩点不一样的，要有剧情 | 剧情引擎+路线规划+密室预订 |
| 企业团建 | 公司20人团建，要会议室和聚餐，预算人均200 | 场地预订+多人+预算控制 |
