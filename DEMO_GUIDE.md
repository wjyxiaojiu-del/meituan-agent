# 演示指南

## 快速开始

```bash
# 安装依赖
pip install -r requirements.txt

# 运行完整演示（3 个场景，约 2 分钟）
python run_demo.py
```

## 演示场景

| 场景 | 输入 | 展示能力 |
|------|------|----------|
| 家庭出游 | 带老婆孩子出去玩，孩子5岁，老婆在减肥，要有惊喜 | 天气+搜索+预订+排队+配送 |
| 剧本杀 Citywalk | 和4个朋友出去玩，想玩点不一样的，要有剧情有挑战 | 剧情引擎+路线规划+密室预订 |
| 企业团建 | 公司20人团建，要会议室和聚餐，预算人均200 | 场地预订+多人+预算控制 |

## 每个场景的完整流程

```
用户输入 → 意图解析 → 路线规划 → 任务生成 → 用户确认 → 工具执行 → 分享文案
```

## 录屏建议

1. **终端窗口**：使用全屏终端，字体调大（16pt+）
2. **配色**：深色背景（黑/深蓝），演示脚本自带颜色输出
3. **节奏**：每个场景暂停 3-5 秒再按回车继续
4. **重点展示**：
   - 路线规划的时间轴（POI 顺序 + 时间递增）
   - 剧本杀模式的故事节点（第1关/第2关/第3关）
   - 执行结果的预订号/排队号/订单号
   - 分享文案的口语化表达

## 备用方案

如果现场网络不可用，Agent 会自动降级到 Mock 模式，演示效果不受影响。

## 文件清单

```
meituan-agent/
├── run_demo.py          # 演示脚本（一键运行 3 场景）
├── config.json          # LLM 配置（可选，不配则 Mock 模式）
├── agent/
│   ├── api.py           # FastAPI 后端
│   └── core/            # 核心引擎
├── miniprogram/         # 微信小程序前端
└── tests/               # 测试（185+ 个）
```
